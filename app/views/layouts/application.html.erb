<!DOCTYPE html>
<html>
  <head>
    <title>Rabbitapp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
  </head>
  <body>
  <nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <%= link_to 'TaskRabit' , root_path, class: "navbar-brand fs-2"%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to 'All Tasks',tasks_path,class:"nav-link active fs-4"%>
        </li>
        <% Category.all.each do |category|%>
        <li class="nav-item">
          <%= link_to category.name,tasks_path(category: category.name),class:"nav-link active fs-4"%>
        </li>
        <% end %>
      </ul>
      <ul class="navbar-nav mx-auto">
        <li class="nav-item">
          <%= link_to 'Create Task',new_task_path,class:"nav-link active fs-4"%>
        </li>
      </ul>
    </div>
  </div>
</nav>
    <div class="container">
    <% flash.each do |type,msg| %>
      <div class="alert alert-<%= type=="notice" ? "success" : "danger"%> alert-dismissible fade show" role="alert">
      <strong>Holy guacamole! <%= msg %></strong>
      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
    <% end %>
    <%= yield %>
    </div>
  </body>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
  <script>
    $('document').ready(function(){ 
      $('.alert').fadeOut(5000);
      $('.btn-close').click(function(){
        $('.alert').slideUp();
      })
     })
  </script>
</html>
